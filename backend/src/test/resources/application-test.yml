spring:
  datasource:
    url: jdbc:h2:mem:gamebuilder;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  ai:
    openai:
      api-key: test-key

app:
  temporal:
    namespace: default
    task-queue: game-generation-test
